name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  ci:
    uses: tschk/ci-recipes/.github/workflows/inauguration-ci.yml@master
    with:
      build-command: "command in --help"
