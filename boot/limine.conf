# Space nanokernel - Limine configuration
# Boots the Space kernel via the Limine boot protocol

TIMEOUT=5

# Verbose boot
VERBOSE=Yes

# Default entry
:Space Kernel
    PROTOCOL=limine
    KERNEL_PATH=boot:///kernel.elf
