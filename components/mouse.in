package space.kernel

// PS/2 mouse and keyboard stub.
// The full driver logic has moved to components/input.in. This file preserves
// the public symbols so kernel-root.in and compositor.in continue to compile.

var mouse-x: Int = 0
var mouse-y: Int = 0
var mouse-buttons: Int = 0

fn mouse-init() -> Int {
  return 0
}

fn mouse-has-data() -> Int {
  return 0
}

fn mouse-process-byte(b: Int) -> Int {
  return 0
}

fn mouse-poll() -> Int {
  return 0
}

fn mouse-left-pressed() -> Int {
  return 0
}

fn mouse-right-pressed() -> Int {
  return 0
}

fn ps2-kbd-init() -> Int {
  return 0
}

fn ps2-kbd-has-data() -> Int {
  return 0
}

fn ps2-scancode-ascii(sc: Int) -> Int {
  return -1
}

fn ps2-kbd-getchar() -> Int {
  return -1
}
